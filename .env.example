# Anthropic API key — get one at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Admin panel password (choose something secure)
ADMIN_PASSWORD=change-me-before-study

# Secret salt for admin cookie (generate with: python -c "import secrets; print(secrets.token_hex(32))")
ADMIN_SECRET_SALT=generate-a-random-string-here
